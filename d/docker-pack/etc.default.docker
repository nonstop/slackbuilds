## Set defaults used by the docker daemon.
## These are flags passed after `dockerd`.

# --data-root - set path for all docker images
# --insecure-registry - allow work with insecure registries
#DOCKER_OPTS="--data-root=/home/docker --insecure-registry=host:port"
